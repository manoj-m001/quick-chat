import { HTMLChakraProps } from "@chakra-ui/react/system";
export interface BreadcrumbLinkProps extends HTMLChakraProps<"a"> {
    /**
     * @default false
     */
    isCurrentPage?: boolean;
}
/**
 * Breadcrumb link.
 *
 * It renders a `span` when it matches the current link. Otherwise,
 * it renders an anchor tag.
 */
export declare const BreadcrumbLink: import("@chakra-ui/react/system").ComponentWithAs<"a", BreadcrumbLinkProps>;
